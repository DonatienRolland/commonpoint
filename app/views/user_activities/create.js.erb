
function refreshForm(activityHTML) {
  const activitys = document.getElementById('not_user_activities_id');
  activitys.insertAdjacentHTML('afterbegin', activityHTML);
}

function addUserActivity(activityHTML) {
  const activitys = document.getElementById('user_activities_id');
  activitys.insertAdjacentHTML('afterbegin', activityHTML);
}


addReview('<%= j render user_activities/show, user: @user, user_activity: @user_activity, act: act %>');
refreshForm('<%= j render user_activities/show, user: @user, user_activity: @user_activity, act: act %>');



