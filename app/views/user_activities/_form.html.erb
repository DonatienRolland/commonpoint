<%= simple_form_for([ user, user_activity ], remote: false) do |f| %>
  <%= f.input :activity_id, input_html: { value: act.id }, as: :hidden  %>
  <%= f.input :level %>
  <%= f.input :description %>
  <%= f.submit "Continuer" %>
<% end %>
